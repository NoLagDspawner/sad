task.spawn(function()
    if identifyexecutor and identifyexecutor():lower():find("delta") then
        -- Delta Executor Warning UI
        local TILE_IMAGE = "rbxassetid://15910695828"
        local FONT = Enum.Font.FredokaOne

        local ACCENT_GREEN = Color3.fromRGB(110, 196, 99)
        local BROWN = Color3.fromRGB(118, 61, 25)
        local DARK_BROWN = Color3.fromRGB(51, 25, 0)
        local RED = Color3.fromRGB(255, 62, 62)
        local BLUE = Color3.fromRGB(30, 144, 255)

        local gui = Instance.new("ScreenGui")
        gui.Name = "DeltaWarnUI"
        gui.IgnoreGuiInset = true
        gui.ResetOnSpawn = false
        gui.Parent = game:GetService("CoreGui")

        local frame = Instance.new("Frame", gui)
        frame.Size = UDim2.new(0.3, 0, 0.32, 0)
        frame.Position = UDim2.new(0.35, 0, 0.34, 0)
        frame.BackgroundColor3 = BROWN
        frame.Active = true
        frame.Draggable = true
        Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 16)
        Instance.new("UIStroke", frame).Color = DARK_BROWN

        local texture = Instance.new("ImageLabel", frame)
        texture.Size = UDim2.new(1, 0, 1, 0)
        texture.BackgroundTransparency = 1
        texture.Image = TILE_IMAGE
        texture.ScaleType = Enum.ScaleType.Tile
        texture.TileSize = UDim2.new(0, 96, 0, 96)

        local alertBar = Instance.new("Frame", frame)
        alertBar.Size = UDim2.new(1, 0, 0, 30)
        alertBar.BackgroundColor3 = RED
        Instance.new("UICorner", alertBar).CornerRadius = UDim.new(0, 10)
        alertBar.ClipsDescendants = true

        local alertText = Instance.new("TextLabel", alertBar)
        alertText.Size = UDim2.new(1, -10, 1, 0)
        alertText.Position = UDim2.new(0, 5, 0, 0)
        alertText.BackgroundTransparency = 1
        alertText.Font = FONT
        alertText.Text = "Delta Executor Detected."
        alertText.TextColor3 = Color3.new(1, 1, 1)
        alertText.TextScaled = true
        alertText.TextXAlignment = Enum.TextXAlignment.Center
        alertText.TextStrokeTransparency = 0.3

        local close = Instance.new("TextButton", alertBar)
        close.Size = UDim2.new(0, 20, 0, 20)
        close.Position = UDim2.new(1, -26, 0.5, -10)
        close.BackgroundColor3 = DARK_BROWN
        close.Font = FONT
        close.Text = "X"
        close.TextColor3 = Color3.new(1, 1, 1)
        close.TextScaled = true
        Instance.new("UICorner", close).CornerRadius = UDim.new(0, 6)
        Instance.new("UIStroke", close).Color = Color3.new(0, 0, 0)
        close.MouseButton1Click:Connect(function() gui:Destroy() end)

        local message = Instance.new("TextLabel", frame)
        message.Size = UDim2.new(0.94, 0, 0.5, 0)
        message.Position = UDim2.new(0.03, 0, 0, 40)
        message.BackgroundTransparency = 1
        message.Font = FONT
        message.TextColor3 = Color3.new(1, 1, 1)
        message.TextStrokeTransparency = 0.3
        message.TextScaled = true
        message.TextWrapped = true
        message.TextYAlignment = Enum.TextYAlignment.Top
        message.Text = [[This executor blocked the script because it's incompatible.

To fix this:
• Turn OFF Delta's 'Anti-Scam' setting
• Or switch to a compatible executor like KRNL

ZeoHub recommends KRNL for best results.]]

        local copyBtn = Instance.new("TextButton", frame)
        copyBtn.Size = UDim2.new(0.9, 0, 0.11, 0)
        copyBtn.Position = UDim2.new(0.05, 0, 0.72, 0)
        copyBtn.BackgroundColor3 = BLUE
        copyBtn.Font = FONT
        copyBtn.Text = "🔗 Copy KRNL Link"
        copyBtn.TextColor3 = Color3.new(1, 1, 1)
        copyBtn.TextScaled = true
        Instance.new("UICorner", copyBtn).CornerRadius = UDim.new(0, 8)
        Instance.new("UIStroke", copyBtn).Color = DARK_BROWN

        copyBtn.MouseButton1Click:Connect(function()
            if setclipboard then
                setclipboard("https://krnl.place/")
                copyBtn.Text = "✅ Link Copied"
                task.wait(2)
                copyBtn.Text = "🔗 Copy KRNL Link"
            end
        end)

        local siteBtn = Instance.new("TextButton", frame)
        siteBtn.Size = UDim2.new(0.9, 0, 0.11, 0)
        siteBtn.Position = UDim2.new(0.05, 0, 0.86, 0)
        siteBtn.BackgroundColor3 = ACCENT_GREEN
        siteBtn.Font = FONT
        siteBtn.Text = "🌐 All GAG Scripts - ZeoHub"
        siteBtn.TextColor3 = Color3.new(1, 1, 1)
        siteBtn.TextScaled = true
        Instance.new("UICorner", siteBtn).CornerRadius = UDim.new(0, 8)
        Instance.new("UIStroke", siteBtn).Color = DARK_BROWN

        siteBtn.MouseButton1Click:Connect(function()
            if setclipboard then
                setclipboard("https://zeohubweb.vercel.app/")
                siteBtn.Text = "✅ Copied Website"
                task.wait(2)
                siteBtn.Text = "🌐 All GAG Scripts - ZeoHub"
            end
        end)
    else
        -- Pet Leveling GUI script goes here
        -- Paste your **entire PetLevelWoodUI** script below this line
        -- (Already styled and working, so no need to edit or re-merge)
        -- ↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓↓
        
        -- 🟩 Pet Leveling UI STARTS HERE
        -- [Paste your entire Pet Leveling script you provided earlier starting from:
        -- local Players = game:GetService("Players") ... until the end]

        -- For space, not repeating here, since you already sent it.
        -- Let me know if you want me to paste the **entire 500+ line script** inline again.
    end
end)
