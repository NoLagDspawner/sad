task.defer(function()
    local TILE_IMAGE = "rbxassetid://15910695828"
    local FONT = Enum.Font.FredokaOne

    local RED = Color3.fromRGB(255, 62, 62)
    local GREEN = Color3.fromRGB(110, 196, 99)
    local BROWN = Color3.fromRGB(118, 61, 25)
    local DARK = Color3.fromRGB(51, 25, 0)
    local BLUE = Color3.fromRGB(30, 144, 255)

    local gui = Instance.new("ScreenGui")
    gui.Name = "WarningUI"
    gui.IgnoreGuiInset = true
    gui.ResetOnSpawn = false
    pcall(function() gui.Parent = game:GetService("CoreGui") end)

    local frame = Instance.new("Frame", gui)
    frame.Size = UDim2.new(0, 420, 0, 200)
    frame.Position = UDim2.new(0.5, -210, 0.5, -100)
    frame.BackgroundColor3 = BROWN
    frame.Active = true
    frame.Draggable = true
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 12)
    Instance.new("UIStroke", frame).Color = DARK

    local texture = Instance.new("ImageLabel", frame)
    texture.Size = UDim2.new(1, 0, 1, 0)
    texture.BackgroundTransparency = 1
    texture.Image = TILE_IMAGE
    texture.ScaleType = Enum.ScaleType.Tile
    texture.TileSize = UDim2.new(0, 96, 0, 96)

    local header = Instance.new("TextLabel", frame)
    header.Size = UDim2.new(1, -20, 0, 36)
    header.Position = UDim2.new(0, 10, 0, 10)
    header.BackgroundTransparency = 1
    header.Font = FONT
    header.Text = "⚠️ Delta Executor Detected"
    header.TextColor3 = Color3.new(1, 1, 1)
    header.TextScaled = true
    header.TextStrokeTransparency = 0.3

    local msg = Instance.new("TextLabel", frame)
    msg.Size = UDim2.new(0.92, 0, 0.55, 0)
    msg.Position = UDim2.new(0.04, 0, 0, 50)
    msg.BackgroundTransparency = 1
    msg.Font = FONT
    msg.TextColor3 = Color3.new(1, 1, 1)
    msg.TextStrokeTransparency = 0.3
    msg.TextScaled = true
    msg.TextWrapped = true
    msg.TextYAlignment = Enum.TextYAlignment.Top
    msg.Text = [[This executor blocked the script.

To fix this:
• Turn OFF Delta’s ‘Anti-Scam’ setting
• Or use KRNL or another safe executor

Recommended: https://krnl.place]]

    local btn = Instance.new("TextButton", frame)
    btn.Size = UDim2.new(0.9, 0, 0, 32)
    btn.Position = UDim2.new(0.05, 0, 1, -42)
    btn.BackgroundColor3 = GREEN
    btn.Font = FONT
    btn.Text = "Visit ZeoHub Website"
    btn.TextColor3 = Color3.new(1, 1, 1)
    btn.TextScaled = true
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)
    Instance.new("UIStroke", btn).Color = DARK

    btn.MouseButton1Click:Connect(function()
        -- Skip setclipboard; use safe prompt
        btn.Text = "Opening..."
        wait(0.5)
        btn.Text = "🌐 https://zeohubweb.vercel.app/"
        wait(2.5)
        btn.Text = "Visit ZeoHub Website"
    end)
end)
